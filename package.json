{
  "name": "ping-bot",
  "version": "1.0.0",
  "description": "Ping web sites and notify subscribers",
  "main": "./dist/server.js",
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.2",
    "node-telegram-bot-api": "^0.64.0"
  },
  "devDependencies": {
    "@types/node-telegram-bot-api": "^0.63.2",
    "typescript": "^5.2.2"
  }
}
